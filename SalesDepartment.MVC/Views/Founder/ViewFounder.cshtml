@using SalesDepartment.Application.UseCases.Founders.Response;

@{
    Layout = "_Layout";
}

@model FounderResponse

<h3 class="text-center">@Model.LastName @Model.FirstName @Model.MiddleName</h3>
<hr />

<div class="container" style="height: 55vh;">
    <div class="row" style="display: flex; justify-content: center;">
        <div class="col-lg-6" style="width: 100%;">
            <div class="custom-cards">
                <div class="table-container" style="overflow-y: scroll; height: 58vh; width: 100%;">
                    <table id="FoundersTable" class="table">
                        <thead>
                            <tr>
                                <th style="width: 25%;">Contract Number</th>
                                <th style="width: 25%;">Total Amount</th>
                                <th style="width: 25%;">Home</th>
                                <th style="width: 25%;">Customer</th>
                                <th style="width: 25%;">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var contract in Model.Contracts)
                            {
                                <tr>
                                    <td>@contract.ContractNumber</td>
                                    <td>@contract.TotalAmountOfContract</td>
                                    <td>@contract.Home.ApartmentNumber</td>
                                    <td>@contract.Customer.FirstName @contract.Customer.LastName</td>
                                    <td class="action-links">
                                        <div style="display: flex; gap: 5px;">
                                            <a href="@Url.Action("ViewContract", "Contract", new { id = contract.Id })" class="btn btn-secondary">Deails</a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
